# Submission assets

Placeholder directory for Anthropic Connector Directory submission assets.

**TODO before submission** — see `docs/CONNECTOR_DIRECTORY_SUBMISSION.md` §9 for full specs:

- `icon.png` — 1024 × 1024 PNG, transparent background. Intentionally **not auto-generated** in this PR; a real design pass is needed so the glyph renders well on light and dark backgrounds and matches the Starter Series brand.
- `icon-512.png` — 512 × 512 PNG (favicon variant).
- `screenshots/01-list-templates.png` — ≥ 1000 px wide PNG, Claude response only (no user prompt visible).
- `screenshots/02-create-project.png` — same constraints.
- `screenshots/03-scaffold-complete.png` — same constraints.

Anthropic's submission spec accepts PNG only for carousel images (no GIF, no video).
